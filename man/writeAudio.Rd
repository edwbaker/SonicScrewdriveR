% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeAudio.R
\name{writeAudio}
\alias{writeAudio}
\title{Write an audio file}
\usage{
writeAudio(wave, file, mime = "auto", ...)
}
\arguments{
\item{wave}{A Wave or WaveMC object to write. The channels of a WaveMC object
must be named, as the name is what records the position of each channel in
the file (see \code{tuneR::MCnames}). Tagged waves (see \code{tagWave()}) are written
as the audio they contain, the tags are not saved.}

\item{file}{File to write.}

\item{mime}{MIME type of the file to write, or "auto" (the default) to
determine it from the file extension (see Details).}

\item{...}{Additional arguments passed to the function performing the write:
\code{tuneR::writeWave()} for WAVE files, and \code{av::av_audio_convert()} (e.g.
\code{bit_rate}) for other formats.}
}
\value{
The path of the file written, invisibly.
}
\description{
This function writes a \code{Wave} or \code{WaveMC} object to a file, it is an
abstraction function for various specific audio writing functions and is
the counterpart of \code{readAudio()}. WAVE files are written directly, other
formats are written by converting a temporary WAVE file using the \code{av}
package. If the \code{av} package is not available FLAC files can also be written
using the \code{flac} program via the \code{seewave} package
\insertCite{seewave2008}{sonicscrewdriver}.
}
\details{
The MIME type of the file to write is determined from the file extension,
unless it is given in the \code{mime} argument. Any format supported by the
encoders available to the \code{av} package can be written, commonly used types
include:
\itemize{
\item \strong{audio/x-wav} (\code{.wav}) - written by \code{tuneR::writeWave()}.
\item \strong{audio/flac} (\code{.flac})
\item \strong{audio/mpeg} (\code{.mp3})
\item \strong{audio/mp4} (\code{.m4a})
}

Which other formats can be written depends on the encoders available to the
\code{av} package, and some encoders accept only certain sample rates (e.g. Opus
requires 48kHz or one of its divisors).

Audio written to a lossy format (e.g. MP3) will not be identical to the audio
held in the object, and audio written to a format that does not support the
bit depth or channel count of the object will be converted by the encoder.
}
\examples{
w <- tuneR::sine(440, duration=1000)
f <- tempfile(fileext=".wav")
writeAudio(w, f)
file.remove(f)

\dontrun{
# Requires the av package
writeAudio(w, "tone.mp3")
writeAudio(w, "tone.flac")
}

}
\references{
\insertAllCited{}
}
