# Create an empty diel plot

Create a diel plot with labels but without sun altitude or times of day
plotted.

## Usage

``` r
emptyDiel(method = "plotrix", rot = pi)
```

## Arguments

- method:

  Plotting package to use

- rot:

  Rotation of the origin (defaults to pi)
