# Converts bytes in human readable form

Given an input of bytes calculates the result in a sensible output unit
(e.g. MB, GB, PB).

## Usage

``` r
humanBytes(S)
```

## Arguments

- S:

  Number of bytes

## Value

String in human readable format
