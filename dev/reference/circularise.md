# Circularise a dataset

When plotting rings or horizons that are meant to cover the entirety of
the time period in a dielPlot() or yearlyPlot() this function append the
beginning values to the end to ensure an entire loop is created.

## Usage

``` r
circularise(values)
```

## Arguments

- values:

  A vector if values
