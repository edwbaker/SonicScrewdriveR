# Converts a timezone offset into a rotation

Given a timezone offset in hours returns a rotation in radians to apply
to values for a diel plot.

## Usage

``` r
tzRot(tz, init = pi)
```

## Arguments

- tz:

  Timezone numeric

- init:

  Initial rotation. Defaults to pi.
