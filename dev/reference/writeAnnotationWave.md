# Save a time region defined by an Annotation object as a Wave file

Save a time region defined by an Annotation object as a Wave file

## Usage

``` r
writeAnnotationWave(annotation, wave = NULL)
```

## Arguments

- annotation:

  An Annotation object.

- wave:

  (Optional) A Wave object, if not given will load the wave file from
  Annotation filename.
